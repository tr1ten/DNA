#include <vector>
class PriorityQueue
{
private:
    /* data */
public:
    std::vector<int> queue;
    int enqueue(int);
    int dequeue();
    PriorityQueue(/* args */);
    ~PriorityQueue();
};
int PriorityQueue::enqueue(int n)
{
    queue
}

PriorityQueue::PriorityQueue(/* args */)
{
}

PriorityQueue::~PriorityQueue()
{
}
